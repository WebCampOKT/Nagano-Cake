<div class="container">
  <div class="row">
    <h4>ジャンル編集</h4>
  </div>
</div>


<%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
  <%= f.label :name, "ジャンル名" %>
  <%=f.text_field :name %>
  <%= f.submit '変更を保存' %>
<% end %>