<table class="border border-dark" style="width: 60%" >
  <thead class="border border-dark">
    <tr class="text-center">
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td class="mb-3"><%= genre.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>